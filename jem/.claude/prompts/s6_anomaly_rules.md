# Session 6 — Anomaly Rules

**Context:** `CLAUDE.md`, `jem/.claude/decisions/schema_lock.md`

## Task

Define operational monitor anomaly detection rules.

For each rule specify:
1. Rule name
2. SQL or logic to detect
3. Severity: `critical` | `warning` | `info`
4. Digest message template

Cover: staging backlog growth, confidence distribution shift, unresolved conflicts, fetch failures, schema drift.

**Output:** Rules table. No implementation.
