# Copy to jem/.env for local runs — never commit .env
# ANTHROPIC_API_KEY is required only for chat harness and fetcher/verifier agents.
# API + MCP entity lookup works without it.

ANTHROPIC_API_KEY=
JEM_DB_PATH=data/jem.db
