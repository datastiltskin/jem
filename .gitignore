.DS_Store
.venv/

# Local secrets (API keys, deploy overrides)
.env
.env.*
!.env.example

# Word exports (regenerate from KNOWLEDGE_TRANSFER.md via pandoc)
*.docx
~$*.docx

# Local deploy snapshots (not source of truth)
_deploy_bundle/

# Personal / draft papers — not for public repo
jem/docs/PAPER_*.md

